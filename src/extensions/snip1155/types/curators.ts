type Snip1155AddCuratorOptions = {
  add_curators: {
    add_curators: string[];
    padding?: string;
  };
};

type Snip1155RemoveCuratorOptions = {
  remove_curators: {
    remove_curators: string[];
    padding?: string;
  };
};
